import { adminUtilUserStats } from './admin-util-user-stats';

describe('adminUtilUserStats', () => {
  it('should work', () => {
    expect(adminUtilUserStats()).toEqual('admin-util-user-stats');
  });
});
